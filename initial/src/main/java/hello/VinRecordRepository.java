package hello;

// import org.springframework.data.mongodb.repository.MongoRepository;

import java.util.List;

/* added for MongoDB
public interface VinRecordRepository extends MongoRepository<VinRecord, String> {

    public VinRecord findByVinNumber  (String vinRecord);
    public List<VinRecord> findByMake (String make);
}
*/
